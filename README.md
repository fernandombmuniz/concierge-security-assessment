# Autosave visual fix

Substitua apenas `src/pages/AssessmentForm.tsx`.

O autosave local e remoto continuam ativos, mas sem alternar o estado visual a cada tecla.

Depois rode:

```powershell
npx tsc --noEmit
npm run build
```
